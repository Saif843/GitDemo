// Key = property

// switch(key){}
// switch(key){}
// switch(key){}
// switch(key){}
// switch(key){
//     case value:

// }


// truthy and falsy value

// Check array length

let userEmail = [];

if(userEmail.length === 0){
    console.log("Array is empty");
}

// Check Object length


let userObj = {}

// Object.keys(userObj) this will return/treates an array now you can use length property 

if(Object.keys(userObj).length === 0){
    console.log("Object is empty");
}


//  Nullish coalescing operator : made for null, undefined 
// Normally has the when we access the values from the database to stop some problem to our code organization

// null and undefined ki jga flag values dal dein
